syntax = "proto3";

package fooapp.proto;

import "code.proto";

enum Code {
  OK = 0;

  NOT_ENOUGH_MONEY = 1;
}

message Status {
  Code code = 1;
  string message = 2;
  map<string, string> extra = 3;
}